---
// links.astro - Links page with sponsorships and referrals
import BaseLayout from '@/layouts/BaseLayout.astro';

const links = {
  support: [
    { title: 'GitHub Sponsors', url: 'https://github.com/sponsors/meysam81', description: 'Support my open source work' },
    { title: 'Buy Me a Coffee', url: 'https://buymeacoffee.com/meysam', description: 'One-time support' },
    { title: 'Patreon', url: 'https://patreon.com/meysam81', description: 'Monthly support' },
  ],
  crypto: [
    { title: 'Bitcoin', url: 'bitcoin:bc1qfn42dnpdderesp5a5fwjx9y70dut4m0p9emx27', description: 'bc1qfn42dnpdderesp5a5fwjx9y70dut4m0p9emx27' },
  ],
  payment: [
    { title: 'PayPal', url: 'https://paypal.me/meysamazad', description: 'Direct payment' },
  ],
  referrals: [
    { title: 'Proton Mail', url: 'https://pr.tn/ref/NS19KFG8SQBG', description: 'Get a free month of Mail Plus' },
    { title: 'Hetzner Cloud', url: 'https://hetzner.cloud/?ref=ai5E5vaX1J71', description: 'Get €20 credit' },
    { title: 'DigitalOcean', url: 'https://m.do.co/c/7de094ea6f0d', description: 'Get $200 credit' },
    { title: 'Maileroo', url: 'https://maileroo.com/?r=developerfriendly', description: 'SMTP/Marketing/Verification' },
    { title: 'Postmark', url: 'https://www.postmarkapp.com/?via=0983a5', description: 'Transactional Email' },
  ],
  social: [
    { title: 'X / Twitter', url: 'https://x.com/meysamazing', description: '@meysamazing' },
    { title: 'LinkedIn', url: 'https://linkedin.com/in/meysamazad', description: 'Professional profile' },
    { title: 'GitHub', url: 'https://github.com/meysam81', description: 'Open source projects' },
    { title: 'YouTube', url: 'https://youtube.com/@meysam', description: 'Video content' },
    { title: 'Instagram', url: 'https://instagram.com/meysamazing', description: '@meysamazing' },
    { title: 'Threads', url: 'http://threads.com/meysamazing', description: '@meysamazing' },
    { title: 'TikTok', url: 'https://tiktok.com/@meysam_new', description: '@meysam_new' },
    { title: 'Mastodon', url: 'https://mastodon.social/@meysam81', description: '@meysam81' },
    { title: 'Bluesky', url: 'https://bsky.app/profile/meysamazing.bsky.social', description: '@meysamazing' },
    { title: 'Telegram', url: 'http://t.me/meysamazing', description: '@meysamazing' },
    { title: 'Product Hunt', url: 'https://producthunt.com/@meysam', description: '@meysam' },
    { title: 'Cal.com', url: 'https://cal.com/meysam', description: 'Schedule a meeting' },
  ],
  contact: [
    { title: 'Email', url: 'mailto:contact@meysam.io', description: 'contact@meysam.io' },
    { title: 'Website', url: 'https://meysam.io', description: 'Personal website' },
  ],
};
---

<BaseLayout
  title="Links - Meysam Azad"
  description="All my links, sponsorships, and ways to connect with me."
>
  <section class="links-hero">
    <div class="container-narrow text-center">
      <h1>All My Links</h1>
      <p class="hero-description">
        Connect with me, support my work, or use these referral links to get great deals on tools I use and recommend.
      </p>
    </div>
  </section>

  <section class="links-content">
    <div class="container-narrow">

      <!-- Support My Work -->
      <div class="links-section">
        <h2 class="section-title">Support My Work</h2>
        <div class="links-grid">
          {links.support.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class="link-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p>{link.description}</p>
              </div>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          ))}
        </div>
      </div>

      <!-- Crypto -->
      <div class="links-section">
        <h2 class="section-title">Cryptocurrency</h2>
        <div class="links-grid">
          {links.crypto.map(link => (
            <div class="link-card crypto-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p class="crypto-address">{link.description}</p>
              </div>
              <button
                class="copy-btn"
                data-copy={link.description}
                aria-label="Copy to clipboard"
              >
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
                  <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
                </svg>
              </button>
            </div>
          ))}
        </div>
      </div>

      <!-- Payment -->
      <div class="links-section">
        <h2 class="section-title">Direct Payment</h2>
        <div class="links-grid">
          {links.payment.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class="link-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p>{link.description}</p>
              </div>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          ))}
        </div>
      </div>

      <!-- Referrals -->
      <div class="links-section">
        <h2 class="section-title">Referral Links (You Get Benefits Too!)</h2>
        <div class="links-grid">
          {links.referrals.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class="link-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p>{link.description}</p>
              </div>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          ))}
        </div>
      </div>

      <!-- Social & Contact -->
      <div class="links-section">
        <h2 class="section-title">Connect With Me</h2>
        <div class="links-grid">
          {links.social.map(link => (
            <a href={link.url} target="_blank" rel="noopener noreferrer" class="link-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p>{link.description}</p>
              </div>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          ))}
          {links.contact.map(link => (
            <a href={link.url} class="link-card">
              <div class="link-info">
                <h3>{link.title}</h3>
                <p>{link.description}</p>
              </div>
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </a>
          ))}
        </div>
      </div>

      <!-- Newsletter CTA -->
      <div class="newsletter-cta">
        <h2>Or Subscribe to My Newsletter</h2>
        <p>Get weekly insights on customer discovery, product validation, and building with DevOps discipline.</p>
        <a href="/#newsletter" class="btn btn-primary">Join 1,500+ Engineers</a>
      </div>

    </div>
  </section>
</BaseLayout>

<style scoped>
  .links-hero {
    padding: var(--space-2xl) 0 var(--space-xl);
    text-align: center;
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto;
  }

  .links-content {
    padding-bottom: var(--space-3xl);
  }

  .links-section {
    margin-bottom: var(--space-3xl);
  }

  .section-title {
    font-size: 1.5rem;
    margin-bottom: var(--space-lg);
    padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--color-border);
  }

  .links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--space-md);
  }

  .link-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-md);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius-sm);
    transition: all var(--transition-base);
    cursor: pointer;
  }

  .link-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
  }

  .link-info {
    flex: 1;
    min-width: 0;
  }

  .link-info h3 {
    font-size: 1rem;
    margin-bottom: 0.25rem;
    color: var(--color-text);
  }

  .link-info p {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .crypto-card {
    cursor: default;
  }

  .crypto-address {
    font-family: var(--font-mono);
    font-size: 0.75rem !important;
    white-space: normal !important;
    word-break: break-all;
  }

  .copy-btn {
    background: transparent;
    border: 1px solid var(--color-border);
    color: var(--color-text-muted);
    padding: 0.5rem;
    border-radius: var(--border-radius-sm);
    cursor: pointer;
    transition: all var(--transition-fast);
    flex-shrink: 0;
  }

  .copy-btn:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
    background: rgba(59, 130, 246, 0.1);
  }

  .copy-btn.copied {
    border-color: var(--color-success);
    color: var(--color-success);
    background: rgba(16, 185, 129, 0.1);
  }

  .newsletter-cta {
    text-align: center;
    padding: var(--space-2xl);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    margin-top: var(--space-2xl);
  }

  .newsletter-cta h2 {
    margin-bottom: var(--space-sm);
  }

  .newsletter-cta p {
    color: var(--color-text-muted);
    margin-bottom: var(--space-lg);
  }

  @media (max-width: 768px) {
    .links-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Copy to clipboard functionality
  document.querySelectorAll('.copy-btn').forEach(button => {
    button.addEventListener('click', async () => {
      const text = button.getAttribute('data-copy');
      if (!text) {return;}

      try {
        await navigator.clipboard.writeText(text);
        button.classList.add('copied');

        const originalHTML = button.innerHTML;
        button.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
        `;

        setTimeout(() => {
          button.classList.remove('copied');
          button.innerHTML = originalHTML;
        }, 2000);
      } catch (err) {

      }
    });
  });
</script>
